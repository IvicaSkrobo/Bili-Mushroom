---
phase: 4
slug: stats-export
status: draft
shadcn_initialized: true
preset: new-york / neutral / cssVariables / lucide
created: 2026-04-15
---

# Phase 4 — UI Design Contract: Stats & Export

> Visual and interaction contract for Phase 4 (Stats dashboard, seasonal calendar,
> per-species stats, PDF export, CSV export). Generated by gsd-ui-researcher.
> Consumed by gsd-ui-checker, gsd-planner, gsd-executor, gsd-ui-auditor.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn/ui (new-york style) |
| Preset | new-york / neutral / cssVariables |
| Component library | Radix UI (via shadcn/ui) |
| Icon library | lucide-react |
| Font — display / headings | Playfair Display (italic serif, Google Fonts) |
| Font — UI copy | DM Sans (Google Fonts) |
| Font — coords / paths / numbers | JetBrains Mono (Google Fonts) |
| Theme mode | Dark-only (Forest Night). `.dark` class on `<html>`. NO light-mode UI in this phase. |

Source: `components.json`, `src/index.css`, CLAUDE.md §Frontend Design, 04-CONTEXT.md

---

## Spacing Scale

Declared values (multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, badge padding, dot indicators |
| sm | 8px | Inline padding, stat card inner gaps |
| md | 16px | Default element spacing, card padding |
| lg | 24px | Section padding, between stat cards row |
| xl | 32px | Between major sections (cards → lists → calendar) |
| 2xl | 48px | Tab content top padding |
| 3xl | 64px | Not used in this phase |

Exceptions:
- Calendar month cells: 12px internal padding (between xs and sm — grid density requirement)
- Touch/click targets on month cells: minimum 44px height for comfortable desktop click
- Stat card number display: 0px letter-spacing override (Playfair Display renders well tight)

Source: 8-point scale default; calendar exception is researcher discretion per D-04.

---

## Typography

4 sizes, 2 weights. No exceptions.

| Role | Font | Size | Weight | Line Height | Usage |
|------|------|------|--------|-------------|-------|
| xs / label / mono | DM Sans / JetBrains Mono | 12px | 400 (regular) | 1.4 | Stat card sub-labels ("TOTAL FINDS"), badge text, export button labels, location coordinates in expanded per-species detail, count badges, secondary metadata |
| Body | DM Sans | 14px | 400 (regular) | 1.5 | Ranked list rows, calendar month names, species names in per-species rows, expanded detail values, all general UI copy |
| Heading | DM Sans | 16px | 700 (bold) | 1.3 | Section headings ("Top Spots", "Best Months", "Per-Species Stats", "Your Season", "Your Species"). Uppercase + tracking 0.12em creates distinction without a third weight. |
| Display | Playfair Display | 36px italic | 700 (bold) | 1.1 | Stat card numerals (the big numbers: 42, 17, etc.) and the page heading "Your Foraging Story" |

Size consolidation rationale:
- 11px (JetBrains Mono coords) → 12px
- 13px (location text) → 12px
- 15px (species name / rank) → 14px (body)
- 20px (calendar month panel header) → 36px display (Playfair Display italic, Forest Codex feel)
- 24px (page heading) → 36px display (same Playfair Display treatment as stat numerals)

Weight consolidation rationale:
- 500 (medium, formerly used on labels) → 400 (regular). Uppercase + tracking provides visual distinction.
- 600 (semibold, formerly used on headings) → 700 (bold). Higher contrast aligns with Forest Codex drama.

Rules:
- Display numerals in stat cards MUST use Playfair Display italic at 36px with amber color — no exceptions (D-01, CONTEXT.md §Specifics).
- Section headings MUST be uppercase with letter-spacing 0.12em (matching established tab nav pattern).
- No Inter, Roboto, system-ui as primary font.

---

## Color

| Role | CSS Variable | Dark Value (oklch) | Hex approx. | Usage |
|------|-------------|-------------------|-------------|-------|
| Dominant (60%) | `--background` | `oklch(0.09 0.016 62)` | `#0F0E09` | Tab background, StatsTab full surface |
| Secondary (30%) | `--card` | `oklch(0.13 0.020 65)` | `#1A1910` | Stat cards, ranked list rows, calendar month cells, per-species rows |
| Amber primary | `--primary` | `oklch(0.78 0.170 78)` | `#D4941A` | Reserved elements only (see below) |
| Muted surface | `--muted` | `oklch(0.17 0.020 65)` | `#252318` | Empty month cells, dividers, secondary row bg |
| Muted text | `--muted-foreground` | `oklch(0.56 0.055 82)` | `#8A7E5C` | Sub-labels, secondary metadata, empty-state text |
| Border | `--border` | `oklch(0.28 0.058 74)` | `#4A4228` | Card borders, row separators, calendar grid lines |
| Destructive | `--destructive` | `oklch(0.58 0.20 25)` | `#C0392B` | Not used in this phase (no destructive actions in stats/export) |
| Forest green secondary | `--secondary` | `oklch(0.24 0.085 145)` | `#1F3A1F` | Best month badge bg, calendar active-month ring |

**Amber (`--primary`) reserved for:**
1. Stat card display numerals (the large Playfair Display numbers)
2. Amber dot / indicator on calendar month cells that have finds
3. Left-border reveal on hovered ranked list rows (matching CollectionTab pattern)
4. Active/expanded state indicator on per-species expandable rows
5. "Export PDF" and "Export CSV" button background (primary action only)
6. Ranked position number in Top Spots and Best Months lists (#1, #2, #3)

Accent must NOT be used on: card borders, dividers, inactive tab labels, help text, sub-labels, the calendar grid itself.

Source: `src/index.css` CSS variables, CLAUDE.md §Frontend Design, 04-CONTEXT.md §Claude's Discretion.

---

## Component Inventory

### Stat Cards (4 cards, STA-01)

Reuse `shadcn/ui` `<Card>` from `src/components/ui/card.tsx`.

Layout: 4-column grid at full tab width. Each card:
- Sub-label: uppercase DM Sans 12px/regular, `--muted-foreground`, tracking 0.12em
- Number: Playfair Display 36px italic bold, `--primary` (amber)
- Optional trend/sub-note: DM Sans 12px regular, `--muted-foreground`

Cards (in order, left to right):
1. **Total Finds** — count of all finds
2. **Unique Species** — distinct species_name count
3. **Locations Visited** — distinct location clusters (grouped by country+region+location_note)
4. **Most Active Month** — month name + year of highest find count (Claude's discretion per D-01)

Hover state: 1px amber left-border reveal (`border-l-2 border-primary opacity-0 group-hover:opacity-100`), matching CollectionTab pattern.

### Ranked Lists: Top Spots + Best Months (STA-02)

Two side-by-side lists below the stat cards (each ~48% width with 16px gap).

Each list row:
- Rank number: Playfair Display italic, amber, 14px (body tier)
- Name: DM Sans 14px regular, `--foreground`
- Count badge: `shadcn/ui` `<Badge>` variant outline, amber border+text, DM Sans 12px
- Amber left-border on hover (3px, `--primary`)
- Row bg: `--card`, border-bottom `--border`

Section headings: DM Sans 16px bold, uppercase, tracking 0.12em, `--foreground`.

### Seasonal Calendar (STA-03)

12-month grid: 3 columns × 4 rows (Jan–Dec reading order).

Each month cell:
- Month name: DM Sans 14px bold, uppercase, `--foreground`
- Find dots: amber filled circles (8px diameter, `--primary` fill) — one dot per unique species found that month, max 5 visible, "+N more" overflow label in `--muted-foreground`
- Empty month (no finds): `--muted` background, month name in `--muted-foreground`, no dots
- Active month with finds: `--card` background, `--border` border, amber dot indicators
- Cell padding: 12px
- Min cell height: 72px

Interaction on month cell click:
- Opens an inline-expanded panel below the grid row (not a modal) OR a `shadcn/ui` `<Dialog>` — executor's discretion based on layout feasibility
- Panel shows: month header (Playfair Display 36px italic, display tier), then a staggered list (`.stagger-item`) of species found that month — species name (Playfair Display italic 14px), date (DM Sans 12px, `--muted-foreground`), location_note (DM Sans 12px, `--muted-foreground`)
- Empty state in panel: "No finds recorded in [Month]." — DM Sans 14px, `--muted-foreground`

### Per-Species Stats (STA-04)

Section below the calendar. Heading: "Your Species" (DM Sans 16px bold uppercase).

Each species row in a ranked list (ranked by find count descending):
- Rank #: Playfair Display italic amber 14px (body tier)
- Species name: Playfair Display italic 14px, `--foreground`
- Find count badge: `<Badge>` outline amber, DM Sans 12px
- Expand chevron: lucide `ChevronDown` / `ChevronUp`, `--muted-foreground`, 16px

Expanded state (click row to toggle, amber left-border appears):
- Total find count: label + number (DM Sans 14px)
- First find date: label + value (DM Sans 14px)
- Best month for species: label + value (DM Sans 14px, amber number)
- Locations: list of `country / region / location_note` strings (DM Sans 12px, `--muted-foreground`, each on own line)

Transition: `max-height` CSS transition, 200ms ease-out. No JS animation library needed.

### Export Section

Placement: Fixed action bar at the bottom of StatsTab (sticky footer within the tab scroll container, above the tab nav bar).

Layout: right-aligned row. Two buttons:
- "Export CSV" — `<Button>` variant outline, DM Sans 14px, `--foreground` text
- "Export PDF" — `<Button>` variant default (amber fill, `--primary-foreground` text), DM Sans 14px bold

Export PDF interaction:
1. User clicks "Export PDF"
2. Button shows loading state: lucide `Loader2` spinning icon (16px), label "Generating PDF..." — button disabled
3. Comlink Web Worker runs `@react-pdf/renderer` render (no UI freeze)
4. On completion: native OS file save dialog via Tauri `save()` dialog (or auto-save to storage root with toast notification — executor's choice based on Tauri dialog plugin availability)
5. On success: button returns to default state. No modal needed unless Tauri save dialog is unavailable.
6. On error: toast (or inline error message below buttons): "PDF export failed. Try again." — DM Sans 14px, `--destructive` color.

Export CSV interaction:
1. User clicks "Export CSV"
2. Instant (no loading state needed for JS Blob CSV) — triggers `<a download>` with Blob URL
3. On success: no explicit feedback needed (browser handles download). If Tauri requires different file write approach, show brief inline "CSV saved to [path]" message below button in `--muted-foreground` DM Sans 12px, fading out after 3 seconds.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | Export PDF |
| Secondary CTA | Export CSV |
| Stats tab heading (page) | Your Foraging Story |
| Stat card 1 label | TOTAL FINDS |
| Stat card 2 label | UNIQUE SPECIES |
| Stat card 3 label | LOCATIONS VISITED |
| Stat card 4 label | MOST ACTIVE MONTH |
| Top Spots section heading | Top Spots |
| Best Months section heading | Best Months |
| Calendar section heading | Your Season |
| Per-species section heading | Your Species |
| Empty state — StatsTab (0 finds) heading | Your story starts with one find |
| Empty state — StatsTab body | Import your first mushroom photo to see your foraging stats here. |
| Empty state — calendar month (no finds) | (no copy — use muted styling only, month name remains visible) |
| Calendar month panel — no finds | No finds recorded in [Month Name]. |
| Export loading state | Generating PDF... |
| Export PDF error | PDF export failed. Try again. |
| Export CSV success (if shown) | CSV saved to [path] |
| Top Spots empty (0 locations) | Start foraging to see your favourite spots appear here. |
| Best Months empty (0 months) | Your most active months will appear here as you record more finds. |
| Per-species empty (0 species) | No species recorded yet. |
| Destructive confirmation | None in this phase — no destructive actions. |

---

## Motion Contract

| Element | Animation | Duration | Easing |
|---------|-----------|----------|--------|
| StatsTab page entry | `.animate-fade-up` on root container | 300ms | ease-out |
| Stat cards | `.stagger-item` with `animationDelay` (0ms, 60ms, 120ms, 180ms) | 220ms each | ease-out |
| Ranked list rows | `.stagger-item` with progressive delay (0ms … N×40ms) | 220ms each | ease-out |
| Per-species row expand | `max-height` CSS transition | 200ms | ease-out |
| Calendar month cell click | No animation on open — instant reveal of panel | — | — |
| Export button loading | lucide `Loader2` CSS `animate-spin` | continuous | linear |
| Export CSV success message | opacity fade-out after 3s (CSS `@keyframes` or `setTimeout` class remove) | 500ms fade | ease-in |

No JS animation libraries. CSS-only. All motion must be interruptible (no blocking sequences).

---

## Interaction States

| Element | Default | Hover | Active/Expanded | Disabled |
|---------|---------|-------|-----------------|----------|
| Stat card | `--card` bg, `--border` border | Amber left-border 2px reveal | N/A | N/A |
| Ranked list row | `--card` bg | Amber left-border 3px, bg `--muted` | N/A | N/A |
| Calendar month cell (with finds) | `--card` bg, dots | `--muted` bg, cursor pointer | Panel open: `--secondary` border ring (forest green) | N/A |
| Calendar month cell (empty) | `--muted` bg, no dots | No hover state | N/A | N/A |
| Per-species row | `--card` bg, chevron down | Amber left-border 2px, cursor pointer | Amber left-border 3px, bg `--muted`, chevron up | N/A |
| Export PDF button | `--primary` bg (amber) | 10% brightness increase | Pressed scale 0.98 | `--muted` bg, `--muted-foreground` text, cursor not-allowed |
| Export CSV button | outline `--border` | `--muted` bg | Pressed scale 0.98 | `--muted` bg, cursor not-allowed |

---

## Layout Structure

```
StatsTab (full tab content area, overflow-y-auto)
├── Page heading: "Your Foraging Story" (Playfair Display 36px italic bold, top padding 48px)
├── Stat cards row (4-column grid, gap-6, px-6)
│   ├── Card 1: Total Finds
│   ├── Card 2: Unique Species
│   ├── Card 3: Locations Visited
│   └── Card 4: Most Active Month
├── Section divider (border-b --border, my-8)
├── Ranked lists row (2-column flex, gap-4, px-6)
│   ├── Top Spots list (flex-1)
│   └── Best Months list (flex-1)
├── Section divider (border-b --border, my-8)
├── Calendar section (px-6)
│   ├── Section heading: "Your Season"
│   └── 3×4 month grid (gap-3)
│       └── [expanded panel inline if month clicked]
├── Section divider (border-b --border, my-8)
├── Per-Species section (px-6)
│   ├── Section heading: "Your Species"
│   └── Expandable species rows list
└── Export action bar (sticky bottom, px-6 py-4, --card bg, border-t --border)
    ├── Helper text: "Export your full collection" (DM Sans 12px, --muted-foreground)
    ├── Export CSV button (outline)
    └── Export PDF button (primary/amber)
```

---

## Registry Safety

| Registry | Blocks / Components Used | Safety Gate |
|----------|--------------------------|-------------|
| shadcn/ui official | Card, Badge, Button, Dialog, Separator (already installed or standard install) | not required |
| Third-party | None declared for this phase | not applicable |

Note: `@react-pdf/renderer` and `comlink` are npm packages, not shadcn registry entries. No registry vetting required.

---

## Design Decisions Reference

| Decision ID | Description | Source |
|-------------|-------------|--------|
| D-01 | Stat cards + simple list layout, no charts in v1 | 04-CONTEXT.md |
| D-02 | All metrics from personal SQLite finds data | 04-CONTEXT.md |
| D-03 | Calendar = personal finds only, not general species season data | 04-CONTEXT.md |
| D-04 | 12-month grid, dots per species, click reveals month species list | 04-CONTEXT.md |
| D-05 | Empty months use muted styling, no dots | 04-CONTEXT.md |
| D-06 | Per-species section lives in StatsTab, not CollectionTab | 04-CONTEXT.md |
| D-07 | Per-species rows expand inline (no modal) | 04-CONTEXT.md |
| D-08 | PDF = journal feel, photos prominent, each find gets section | 04-CONTEXT.md |
| D-09 | Full collection export only (no filtered subset) | 04-CONTEXT.md |
| D-10 | @react-pdf/renderer + Comlink Web Worker for PDF | 04-CONTEXT.md / CLAUDE.md |
| D-11 | Flat CSV, full collection, JS Blob export | 04-CONTEXT.md |
| R-01 | Stat numerals = Playfair Display italic amber (celebratory) | 04-CONTEXT.md §Specifics |
| R-02 | Calendar dots = amber, mushroom feel (not generic calendar widget) | 04-CONTEXT.md §Specifics |
| R-03 | Export button placement = StatsTab sticky footer bar | Researcher discretion per D-09 |
| R-04 | 4th stat card = Most Active Month | Researcher discretion per D-01 |
| R-05 | Typography: 4 sizes (12/14/16/36px), 2 weights (400/700) | Checker revision 2026-04-15 |

---

## Pre-Population Sources

| Section | Source | Pre-populated |
|---------|--------|--------------|
| Design system | components.json + src/index.css | Yes — shadcn new-york, lucide, Tailwind v4 |
| Typography | CLAUDE.md Forest Codex + checker revision | Yes — Playfair / DM Sans / JetBrains Mono; 4 sizes, 2 weights |
| Color | src/index.css .dark variables | Yes — all exact oklch values |
| Spacing scale | 8-point default + researcher judgment | Yes — standard scale + calendar exception |
| Stat card design | 04-CONTEXT.md D-01 + §Specifics | Yes |
| Calendar layout | 04-CONTEXT.md D-03, D-04, D-05 | Yes |
| Per-species | 04-CONTEXT.md D-06, D-07 | Yes |
| PDF export | 04-CONTEXT.md D-08, D-09, D-10 | Yes |
| CSV export | 04-CONTEXT.md D-11 | Yes |
| Copywriting | Researcher discretion (consistent with Forest Codex tone) | Yes — no user input required |
| Motion | Existing CSS patterns from src/index.css | Yes |
| Export placement | Researcher discretion | Yes — sticky footer action bar |
| 4th stat card | Researcher discretion | Yes — Most Active Month |
| User input required | None — all upstream artifacts + defaults sufficient | 0 questions asked |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
