---
status: complete
phase: 02.1-import-workflow-refinements
source: [02.1-01-SUMMARY.md, 02.1-02-SUMMARY.md, 02.1-03-SUMMARY.md, 02.1-04-SUMMARY.md, 02.1-05-SUMMARY.md]
started: 2026-04-11T09:40:00Z
updated: 2026-04-11T10:15:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Delete Find — Record Only
expected: In Collection tab, hover a find card — delete button appears. Click it. Alert dialog opens with two radio options: "Delete record only" (selected by default) and "Delete record + files". Click Confirm. Find disappears from list. File remains on disk.
result: pass

### 2. Delete Find — Record + Files
expected: Open delete dialog for a find. Select "Delete record + files". Click Confirm. Find disappears from list. The original photo file is moved to system Recycle Bin (not permanently deleted — recoverable).
result: pass

### 3. Pick Location on Map (Import)
expected: Open import dialog, add a photo. In the preview card, the lat/lng row shows a map pin icon button on the right side. Clicking it opens a map dialog centered on Croatia [45.1°, 15.2°].
result: pass

### 4. Map Click Sets Lat/Lng
expected: With the location picker map open, click anywhere on the map. A marker appears at the clicked spot. Click Confirm. The map closes. The lat and lng fields in the import card update to reflect the clicked coordinates.
result: pass

### 5. Post-Import Review Dialog Opens
expected: In import dialog, fill in at least one photo card with a species name and date. Click "Import All". The ImportDialog does NOT close immediately. Instead, a review dialog opens on top titled something like "Import complete — 1 imported".
result: pass

### 6. Review Dialog Shows Imported and Skipped
expected: The review dialog lists each imported find (species name or filename + date). If any files were skipped during import, a separate "Skipped" section shows the skipped filenames. If nothing was skipped, no skipped section appears.
result: pass

### 7. Inline Edit from Review Dialog
expected: In the post-import review dialog, click the "Edit" button next to an imported find. An edit dialog (or inline form) opens pre-filled with that find's data. Make a change, save it. The review dialog is still visible in the background.
result: pass

### 8. Done Button Closes Everything
expected: Click "Done" in the review dialog. The review dialog closes. The ImportDialog also closes. The collection list refreshes to show the newly imported finds.
result: pass

### 9. BulkMetadataBar Appears for Batches
expected: In the import dialog, add 2 or more photos. Above the preview cards, a "Apply to all X cards:" bar appears with Species name, Date, and Country fields. With only 1 photo, this bar is NOT shown.
result: skipped
reason: BulkMetadataBar was removed — shared header cascade replaced it as the primary bulk approach (quick task 260411-uat-review-fixes).

### 10. Apply to All Pushes Fields
expected: With 2+ photos in import and BulkMetadataBar visible, type a species name into the bar's Species field. Click "Apply to all". All preview cards update their Species field to the typed value. The bar's inputs reset to empty afterward.
result: skipped
reason: BulkMetadataBar removed — see test 9.

## Summary

total: 10
passed: 8
issues: 0
pending: 0
skipped: 2

## Gaps

[none]
