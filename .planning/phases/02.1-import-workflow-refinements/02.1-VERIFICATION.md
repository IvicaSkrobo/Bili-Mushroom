---
phase: 02.1-import-workflow-refinements
verified: 2026-04-10T02:00:00Z
status: gaps_found
score: 6/11 must-haves verified
overrides_applied: 0
gaps:
  - truth: "LocationPickerMap is wired into the import dialog so users can pick a location on a map during import"
    status: failed
    reason: "LocationPickerMap component exists and passes tests, but ImportDialog/FindPreviewCard still uses manual lat/lng text inputs only — map picker is never opened from the import flow"
    artifacts:
      - path: "src/components/import/ImportDialog.tsx"
        issue: "No import or usage of LocationPickerMap; lat/lng only collected via text inputs"
      - path: "src/components/import/FindPreviewCard.tsx"
        issue: "No 'Pick on map' button or LocationPickerMap integration"
    missing:
      - "Wire LocationPickerMap into FindPreviewCard or ImportDialog with a 'Pick on map' button that passes onConfirm lat/lng back to the payload"

  - truth: "Post-import review dialog shows the batch of finds after import completes"
    status: failed
    reason: "No PostImportReviewDialog component exists anywhere in the codebase"
    artifacts:
      - path: "src/components/import/PostImportReviewDialog.tsx"
        issue: "File does not exist"
    missing:
      - "Create PostImportReviewDialog component that receives the ImportSummary and lets users review each imported find"
      - "Wire into ImportDialog to open after successful import"

  - truth: "User can apply bulk metadata (e.g. species name, date, location) across multiple selected finds in a single operation"
    status: failed
    reason: "No BulkMetadataBar or bulk edit capability exists anywhere in the codebase"
    artifacts:
      - path: "src/components/import/BulkMetadataBar.tsx"
        issue: "File does not exist"
    missing:
      - "Create BulkMetadataBar component that allows setting shared fields across all finds in the import batch"
      - "Wire into ImportDialog"

  - truth: "CollectionTab shows delete find UI allowing user to delete a find with optional disk deletion"
    status: failed
    reason: "useDeleteFind hook exists but no DeleteFindDialog or delete UI exists in CollectionTab or FindCard"
    artifacts:
      - path: "src/components/finds/DeleteFindDialog.tsx"
        issue: "File does not exist"
    missing:
      - "Create DeleteFindDialog with 'Delete record only' / 'Delete record and files' options"
      - "Wire useDeleteFind hook into CollectionTab or FindCard"

  - truth: "Find card shows photo strip for multi-photo finds"
    status: failed
    reason: "FindCard/CollectionTab components have not been updated to render the photos array returned by get_finds"
    artifacts: []
    missing:
      - "Update FindCard or CollectionTab to render find.photos as a photo strip (at minimum show primary photo)"
human_verification:
  - test: "SQLite migration 0003 runs against an existing DB and migrates existing rows"
    expected: "App starts with existing DB, find_photos table created, existing photo_path rows appear in find_photos with is_primary=1"
    why_human: "Requires a live SQLite DB with pre-existing data; cannot verify in-memory tests cover production migration path"
  - test: "Location picker map renders in import flow and pin drop sets coordinates"
    expected: "After wiring: click 'Pick on map' in import dialog, Leaflet map renders, clicking sets lat/lng in the form"
    why_human: "Requires browser rendering of Leaflet map; jsdom cannot render canvas/WebGL"
  - test: "Delete find with 'Delete files' option moves photo file to Recycle Bin on Windows"
    expected: "File appears in Windows Recycle Bin after delete; trash::delete called with correct abs path"
    why_human: "Requires Windows shell API; cannot test in CI/macOS environment"
---

# Phase 02.1: Import Workflow Refinements — Verification Report

**Phase Goal:** Multi-photo finds, map-based location picking during import, bulk metadata, post-import review dialog, and delete find with optional disk delete
**Verified:** 2026-04-10T02:00:00Z
**Status:** gaps_found
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Migration 0003 creates find_photos table and migrates existing photo_path data | VERIFIED | `src-tauri/migrations/0003_find_photos.sql` contains CREATE TABLE, INSERT INTO find_photos, ALTER TABLE finds DROP COLUMN photo_path. Test `test_migration_0003_migrates_existing_photo_path` passes. |
| 2 | All Rust commands compile without referencing finds.photo_path column | VERIFIED | `cargo test` 37 tests pass; FindRecord has no photo_path field; import.rs INSERT SQL verified. |
| 3 | import_find handles additional_photos for Mode A multi-photo finds | VERIFIED | ImportPayload.additional_photos field exists; import_find loops over it, copies files, inserts with is_primary=false. |
| 4 | delete_find removes DB record and optionally moves photos to system trash | VERIFIED | finds.rs delete_find: PRAGMA foreign_keys ON, trash::delete called when delete_files=true, DELETE FROM finds. Tests confirm cascade. |
| 5 | get_find_photos returns photo records for a given find_id | VERIFIED | finds.rs get_find_photos: SELECT with ORDER BY is_primary DESC, id ASC. Test passes. |
| 6 | get_finds returns finds with embedded photos array | VERIFIED | Two-query pattern in import.rs: HashMap<i64, Vec<FindPhoto>> built from find_photos, attached to each FindRecord. |
| 7 | leaflet, react-leaflet, and @types/leaflet are installed | VERIFIED | package.json: leaflet@^1.9.4, react-leaflet@^4.2.1 (v4 used — v5 requires React 19, v4 is correct), @types/leaflet@^1.9.21. |
| 8 | Find TypeScript type has photos array instead of photo_path | VERIFIED | finds.ts: Find interface has `photos: FindPhoto[]`, no photo_path field. FindPhoto interface present. |
| 9 | LocationPickerMap renders a Leaflet map in a dialog and returns lat/lng on confirm | VERIFIED | Component exists at 93 lines with MapContainer, TileLayer, Marker, ClickHandler, onConfirm callback. 4 tests pass with mocked react-leaflet. |
| 10 | tauri-mocks dispatch table has delete_find and get_find_photos handlers | VERIFIED | tauri-mocks.ts: delete_find and get_find_photos handlers present; update_find mock has photos:[] instead of photo_path. |
| 11 | useDeleteFind hook exists for TanStack Query mutation | VERIFIED | useFinds.ts: useDeleteFind uses useMutation, calls deleteFind, invalidates finds query on success. |
| 12 | LocationPickerMap is wired into the import dialog so users can pick a location on a map during import | FAILED | ImportDialog.tsx and FindPreviewCard.tsx have no LocationPickerMap import or usage. Manual lat/lng text inputs only. |
| 13 | Post-import review dialog shows the batch of finds after import completes | FAILED | No PostImportReviewDialog component exists. VALIDATION.md planned this as Phase 02.1 Task `post-import-review` (Wave 3). |
| 14 | User can apply bulk metadata across multiple finds in a single operation | FAILED | No BulkMetadataBar component exists. VALIDATION.md planned this as Phase 02.1 Task `bulk-metadata-bar` (Wave 3). |
| 15 | CollectionTab shows delete find UI so user can trigger deletion with optional disk delete | FAILED | No DeleteFindDialog or delete UI wired in CollectionTab/FindCard. useDeleteFind hook exists but is unused. |
| 16 | Find card renders the photos array so multi-photo finds show a photo strip | FAILED | VALIDATION.md planned `find-card-photo-strip` as Phase 02.1 Task (Plan 03). No photo strip or photos rendering in FindCard. |

**Score:** 11/16 truths verified (infrastructure complete, integration not done)

Note: The 11 plan must-haves from Plans 01 and 02 frontmatter all pass. The additional 5 failures come from the phase goal's user-facing features that the VALIDATION.md confirms were planned as Phase 02.1 tasks (import-dialog-refactor, bulk-metadata-bar, post-import-review, find-card-photo-strip, delete-find-ui) but were never executed.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src-tauri/migrations/0003_find_photos.sql` | Schema migration for find_photos table | VERIFIED | 25 lines; CREATE TABLE, INSERT migration, DROP COLUMN photo_path, schema_version=3 |
| `src-tauri/src/commands/finds.rs` | delete_find and get_find_photos commands | VERIFIED | 151 lines; both commands present with PRAGMA FK and trash::delete |
| `src-tauri/src/commands/import.rs` | Updated import_find, get_finds, update_find without photo_path | VERIFIED | FindRecord has photos: Vec<FindPhoto>; two-query get_finds with HashMap |
| `src/components/map/LocationPickerMap.tsx` | Reusable click-to-pin map picker modal | VERIFIED | 93 lines; full implementation with dialog, map, ClickHandler, confirm/cancel |
| `src/components/map/LocationPickerMap.test.tsx` | Tests for map picker callbacks | VERIFIED | 84 lines; 4 tests with mocked react-leaflet and leaflet |
| `src/lib/finds.ts` | Updated Find type with photos[], FindPhoto type, deleteFind and getFindPhotos wrappers | VERIFIED | All exports present: Find, FindPhoto, deleteFind, getFindPhotos |
| `src/components/import/PostImportReviewDialog.tsx` | Post-import review dialog | MISSING | File does not exist |
| `src/components/import/BulkMetadataBar.tsx` | Bulk metadata bar for import batch | MISSING | File does not exist |
| `src/components/finds/DeleteFindDialog.tsx` | Delete find UI with disk option | MISSING | File does not exist |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `src-tauri/src/lib.rs` | `src-tauri/src/commands/finds.rs` | invoke_handler registration | WIRED | `commands::finds::delete_find` and `commands::finds::get_find_photos` both registered |
| `src-tauri/migrations/0003_find_photos.sql` | `src-tauri/src/commands/import.rs` | schema dependency | WIRED | import.rs uses find_photos table in all queries; no photo_path in FindRecord |
| `src/components/map/LocationPickerMap.tsx` | `react-leaflet` | MapContainer, TileLayer, Marker, useMapEvents imports | WIRED | All 4 imports present from 'react-leaflet' |
| `src/lib/finds.ts` | `@tauri-apps/api/core` | invoke wrappers | WIRED | deleteFind invokes 'delete_find'; getFindPhotos invokes 'get_find_photos' |
| `src/main.tsx` | `leaflet/dist/leaflet.css` | CSS import for map styling | WIRED | `import 'leaflet/dist/leaflet.css'` on line 4, before index.css |
| `src/components/import/ImportDialog.tsx` | `LocationPickerMap` | map picker integration | NOT WIRED | ImportDialog has no LocationPickerMap import; FindPreviewCard uses text inputs only |
| `src/hooks/useFinds.ts` | any UI component | useDeleteFind hook used | ORPHANED | useDeleteFind exported but not imported anywhere in production code |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `LocationPickerMap.tsx` | `pin` (L.LatLng) | Map click event via ClickHandler | Yes — real click coordinates | FLOWING (when open) |
| `useDeleteFind` hook | mutation result | `deleteFind` IPC → Rust `delete_find` | Yes — real DB delete | FLOWING (but hook is orphaned — never called from UI) |
| `get_finds` response | `records[].photos` | Two-query HashMap join from find_photos | Yes — real DB rows | FLOWING to wherever getFinds is called |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Rust: 37 tests pass | `cargo test` (src-tauri) | `test result: ok. 37 passed; 0 failed` | PASS |
| Frontend: 101 tests pass | `npm run test -- --run` | `Tests 101 passed | 1 skipped` | PASS |
| LocationPickerMap: 4 tests pass | included in npm run test | All 4 LocationPickerMap tests green | PASS |
| finds.rs: 3 tests pass | included in cargo test | test_delete_find_*, test_get_find_photos_* all pass | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|---------|
| IMP-01 | Plan 01 | Batch import with multi-photo support | SATISFIED | import_find handles additional_photos Vec; get_finds returns photos array |
| IMP-04 | Plan 02 | GPS EXIF fallback to manual location picker on map | PARTIAL | LocationPickerMap component ready but NOT wired into import dialog |
| ORG-04 | Plan 01 | User can edit find after import (includes delete) | PARTIAL | delete_find Rust command + useDeleteFind hook exist but no delete UI in CollectionTab/FindCard |
| IMP-02 | ROADMAP | Single photo file picker | NOT VERIFIED IN THIS PHASE | Listed in ROADMAP as Phase 02.1 requirement; no new single-pick work done — pre-existing from Phase 2 |
| IMP-05 | ROADMAP | Preview metadata before confirming import | NOT VERIFIED IN THIS PHASE | Listed as Phase 02.1 requirement; BulkMetadataBar and PostImportReviewDialog planned but not built |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `src/hooks/useFinds.ts` | 25-35 | `useDeleteFind` exported but not used in any production component | Warning | Hook is complete and correct; needs wiring to a delete UI — not a code bug, a missing integration |
| `src/components/import/FindPreviewCard.tsx` | 28-33 | Manual lat/lng text inputs without map picker trigger | Warning | User-facing gap: no 'Pick on map' button despite LocationPickerMap being ready |

No TODO/FIXME/placeholder comments or empty return() patterns found in the implemented files.

### Human Verification Required

#### 1. SQLite Migration 0003 Against Existing DB

**Test:** Start the app with an existing database that has rows with photo_path, then inspect the SQLite file
**Expected:** find_photos table exists, photo_path rows appear as find_photos rows with is_primary=1, finds.photo_path column is gone
**Why human:** Requires live Tauri app with an existing database; in-memory migration tests pass but the tauri-plugin-sql migration runner uses a different code path

#### 2. Delete Find With Files — Windows Recycle Bin

**Test:** On Windows, import a photo, then delete the find with "Delete files" checked
**Expected:** File appears in Windows Recycle Bin; Rust trash::delete called with correct absolute path
**Why human:** trash crate uses Windows Shell API; cannot test on macOS CI environment

#### 3. Location Picker Map Renders (after wiring is done)

**Test:** After LocationPickerMap is wired into ImportDialog, click "Pick on map" during import
**Expected:** Map dialog opens, clicking a point sets a pin, clicking Confirm populates lat/lng in the form
**Why human:** Leaflet map requires real browser rendering; jsdom cannot render Leaflet canvas/SVG

### Gaps Summary

Phase 02.1 delivered solid infrastructure in two executed plans: the Rust backend (migration 0003, find_photos schema, delete_find, get_find_photos, updated import/get_finds) and the frontend foundation (TypeScript types, LocationPickerMap component, useDeleteFind hook, tauri-mocks). All 37 Rust tests and 101 frontend tests pass.

However, the phase **goal** names five user-facing features. Three of them were never built:

1. **Map-based location picking during import** — LocationPickerMap exists but is not wired into ImportDialog or FindPreviewCard. The import flow still uses plain text inputs for lat/lng.

2. **Post-import review dialog** — PostImportReviewDialog was planned in VALIDATION.md as a Wave 3 task but no file was created.

3. **Bulk metadata** — BulkMetadataBar was planned in VALIDATION.md as a Wave 3 task but no file was created.

Additionally, two integration tasks from the original VALIDATION.md plan were not executed:

4. **Find card photo strip** — No photo strip renders in FindCard/CollectionTab despite get_finds returning photos array.

5. **Delete find UI** — useDeleteFind hook exists but no DeleteFindDialog or delete trigger exists in the collection view.

The VALIDATION.md confirms these were intended to be part of Phase 02.1 (tasks: import-dialog-refactor, bulk-metadata-bar, post-import-review, find-card-photo-strip, delete-find-ui), but the ROADMAP was updated to show 2/2 plans complete before these tasks were executed. The phase was declared done prematurely from a goal-achievement perspective.

---

_Verified: 2026-04-10T02:00:00Z_
_Verifier: Claude (gsd-verifier)_
