---
phase: 02-import-organization
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/lib/finds.ts
  - src/components/import/ImportDialog.tsx
  - src/components/import/ImportDialog.test.tsx
  - src/components/import/FindPreviewCard.tsx
  - src/components/import/FindPreviewCard.test.tsx
  - src/components/import/useImportProgress.ts
  - src/components/ui/card.tsx
  - src/components/ui/input.tsx
  - src/components/ui/progress.tsx
  - src/components/ui/textarea.tsx
  - src/test/tauri-mocks.ts
autonomous: true
requirements:
  - IMP-01
  - IMP-02
  - IMP-03
  - IMP-04
  - IMP-05
  - ORG-01
  - ORG-03
must_haves:
  truths:
    - "User clicks an 'Import Photos' trigger and a dialog lets them pick either a file or a folder of images"
    - "For each picked image, a FindPreviewCard renders with thumbnail, species, date, country, region, lat, lng, notes — all editable"
    - "When EXIF has date/GPS, the preview card pre-fills date/lat/lng; otherwise those fields are empty and user-editable"
    - "Clicking 'Import All' invokes the Rust `import_find` command with the edited payloads and shows a progress bar updating from the `import-progress` event"
    - "On completion a success toast reports imported/skipped counts and the dialog closes"
    - "HEIC/HEIF files render a placeholder icon instead of a broken image (WebView2 cannot render HEIC)"
    - "Verifies the migration key risk (A5) with a runtime integration test that the finds table is reachable via the absolute-path DB connection after launch"
  artifacts:
    - path: "src/lib/finds.ts"
      provides: "TypeScript types for ExifData/ImportPayload/FindRecord/ImportSummary + invoke wrappers + useImportProgress listener hook"
      exports: ["Find", "ImportPayload", "parseExif", "importFind", "getFinds"]
    - path: "src/components/import/ImportDialog.tsx"
      provides: "shadcn Dialog with file/folder picker + preview list + Import All + progress bar"
      exports: ["ImportDialog"]
    - path: "src/components/import/FindPreviewCard.tsx"
      provides: "editable card per pending import"
      exports: ["FindPreviewCard"]
    - path: "src/components/import/useImportProgress.ts"
      provides: "useEffect-wrapped tauri listen() for 'import-progress' event with cleanup"
      exports: ["useImportProgress"]
  key_links:
    - from: "src/components/import/ImportDialog.tsx"
      to: "src/lib/finds.ts"
      via: "importFind + parseExif calls"
      pattern: "importFind\\(|parseExif\\("
    - from: "src/components/import/ImportDialog.tsx"
      to: "src/components/import/useImportProgress.ts"
      via: "progress event subscription"
      pattern: "useImportProgress"
    - from: "src/components/import/FindPreviewCard.tsx"
      to: "@tauri-apps/api/core convertFileSrc"
      via: "thumbnail src"
      pattern: "convertFileSrc"
    - from: "src/lib/finds.ts"
      to: "Rust commands"
      via: "invoke('parse_exif' | 'import_find' | 'get_finds')"
      pattern: "invoke<"
---

<objective>
Build the React import UI: a dialog that opens the Tauri file/folder picker, runs `parse_exif` on each selected photo, renders an editable preview card list, and calls `import_find` with progress tracking. This plan consumes the Rust contracts shipped in Plan 01 and exposes the `finds.ts` TypeScript wrappers that Plan 03 will reuse.

Purpose: Deliver the end-to-end "import" interaction. CollectionTab itself stays untouched in this plan — it gains only a temporary "Import Photos" button trigger that Plan 03 will replace with the full find list. The goal here is the dialog, the preview card, the progress listener, and the TypeScript contract layer.

Output: `finds.ts` wrapper module, ImportDialog + FindPreviewCard + useImportProgress hook, shadcn Card/Input/Progress/Textarea components added if missing, Vitest tests using `mockIPC` to fake the Rust commands, and an integration test that opens the real Database via absolute path and asserts the finds table exists (closes A5).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/02-import-organization/02-CONTEXT.md
@.planning/phases/02-import-organization/02-RESEARCH.md
@.planning/phases/02-import-organization/02-VALIDATION.md
@.planning/phases/02-import-organization/02-01-PLAN.md
@src/lib/db.ts
@src/stores/appStore.ts
@src/tabs/CollectionTab.tsx
@src/components/dialogs/SettingsDialog.tsx
@src/test/tauri-mocks.ts

<interfaces>
<!-- Contracts from Plan 01 (Rust) that this plan calls via invoke(). -->

```typescript
// src/lib/finds.ts — NEW, mirrors Plan 01 Rust structs

export interface ExifData {
  date: string | null;   // ISO YYYY-MM-DD
  lat: number | null;
  lng: number | null;
}

export interface ImportPayload {
  source_path: string;
  original_filename: string;
  species_name: string;
  date_found: string;     // ISO YYYY-MM-DD
  country: string;
  region: string;
  lat: number | null;
  lng: number | null;
  notes: string;
}

export interface Find {
  id: number;
  photo_path: string;
  original_filename: string;
  species_name: string;
  date_found: string;
  country: string;
  region: string;
  lat: number | null;
  lng: number | null;
  notes: string;
  created_at: string;
}

export interface ImportSummary {
  imported: Find[];
  skipped: string[];
}

export interface ImportProgress {
  current: number;
  total: number;
  filename: string;
}

// Wrappers use @tauri-apps/api/core invoke — note snake_case Rust command names,
// but @tauri-apps/api/core auto-converts camelCase JS args to snake_case Rust args.
export async function parseExif(path: string): Promise<ExifData>;
export async function importFind(storagePath: string, payloads: ImportPayload[]): Promise<ImportSummary>;
export async function getFinds(storagePath: string): Promise<Find[]>;
```

Shadcn components available from Phase 1: Dialog, Tabs, Button, Alert. MISSING (add via `npx shadcn@latest add`): Card, Input, Progress, Textarea, Badge (Badge also used in Plan 03).

Existing test mock infrastructure: `src/test/tauri-mocks.ts` mocks `@tauri-apps/api/core` invoke per Phase 1. Extend it with mock handlers for `parse_exif`, `import_find`, `get_finds`.

AppStore: `useAppStore(s => s.storagePath)` provides the absolute StorageRoot path needed by importFind/getFinds.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install shadcn components + finds.ts wrappers + extend tauri-mocks + DB integration test (A5 closure)</name>
  <files>
    src/components/ui/card.tsx,
    src/components/ui/input.tsx,
    src/components/ui/progress.tsx,
    src/components/ui/textarea.tsx,
    src/components/ui/badge.tsx,
    src/lib/finds.ts,
    src/lib/finds.test.ts,
    src/test/tauri-mocks.ts,
    src/lib/db.integration.test.ts
  </files>
  <behavior>
    finds.test.ts (Vitest, mockIPC):
    - parseExif('/tmp/a.jpg') calls invoke('parse_exif', { path: '/tmp/a.jpg' }) and returns ExifData
    - importFind('/storage', [payload]) calls invoke('import_find', { storagePath: '/storage', payloads: [payload] }) and returns ImportSummary
    - getFinds('/storage') calls invoke('get_finds', { storagePath: '/storage' }) and returns Find[]
    - All three propagate errors by rejecting with the raw string from Rust

    db.integration.test.ts (A5 regression — mockIPC cannot execute SQL, so this test runs only when the real Tauri runtime is available. In Vitest it is marked `describe.skipIf(!process.env.TAURI_INTEGRATION)` so it compiles but no-ops on CI. It documents the manual verification path):
    - Skipped by default with a clear comment pointing to the manual smoke step in 02-VALIDATION.md.
    - When TAURI_INTEGRATION=1, it opens Database.load via the same absolute path scheme, runs `SELECT name FROM sqlite_master WHERE type='table' AND name='finds'`, and asserts exactly one row.
    - NOTE: the real A5 guarantee comes from the Rust cargo test in Plan 01 Task 2 (migration-key regression test). This file is an auxiliary integration point when a developer wants end-to-end confidence.
  </behavior>
  <action>
    1. Install shadcn components: run `npx shadcn@latest add card input progress textarea badge` (non-interactive; accepts defaults from components.json already in repo). Verify files land under `src/components/ui/`.
    2. Create `src/lib/finds.ts`:
       - Define all interfaces listed in <interfaces>.
       - Implement wrappers using `import { invoke } from '@tauri-apps/api/core';`.
       - `parseExif`: `return invoke<ExifData>('parse_exif', { path });`
       - `importFind`: `return invoke<ImportSummary>('import_find', { storagePath, payloads });`
       - `getFinds`: `return invoke<Find[]>('get_finds', { storagePath });`
       - Export a `SUPPORTED_EXTENSIONS = ['.jpg','.jpeg','.png','.webp','.heic','.heif']` const.
       - Export a `isHeic(filename: string): boolean` helper.
    3. Extend `src/test/tauri-mocks.ts` with default handlers for the three new commands. Follow the existing pattern from Phase 1 (look for the `mockIPC` / custom handler setup). Handlers should be overridable per-test via a registry object.
    4. Write `src/lib/finds.test.ts` asserting argument shape and return values for all three wrappers (4+ tests).
    5. Create `src/lib/db.integration.test.ts` as specified in <behavior>: a describe block that is `describe.skipIf(!process.env.TAURI_INTEGRATION)` with a single smoke test + a leading comment block explaining that the Rust-side migration regression test in Plan 01 Task 2 is the authoritative A5 guard.
    6. Run `npm test -- --run` — all new tests green, Phase 1's 24 tests still green.
  </action>
  <verify>
    <automated>npm test -- --run</automated>
  </verify>
  <done>
    shadcn Card/Input/Progress/Textarea/Badge added, finds.ts exports all three wrappers with tests green, tauri-mocks extended with default handlers, DB integration test file present (skipped by default with clear documentation), full Vitest suite green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FindPreviewCard + useImportProgress hook with tests</name>
  <files>
    src/components/import/FindPreviewCard.tsx,
    src/components/import/FindPreviewCard.test.tsx,
    src/components/import/useImportProgress.ts,
    src/components/import/useImportProgress.test.tsx
  </files>
  <behavior>
    FindPreviewCard.test.tsx:
    - Renders thumbnail `<img>` with src from `convertFileSrc(sourcePath)` for a .jpg file
    - Renders a HEIC placeholder (lucide Image icon inside a muted box, not an `<img>`) for a .heic file
    - All fields (species, date, country, region, lat, lng, notes) are rendered as editable Input/Textarea
    - Typing into an input fires onChange with the updated payload
    - Empty date shows a warning hint "Date required"
    - Empty lat/lng does NOT show a warning (optional fields)
    - An 'X' remove button fires onRemove

    useImportProgress.test.tsx:
    - Subscribes to `import-progress` event on mount and calls setter with payload
    - Unsubscribes on unmount (verifies unlisten fn is called)
    - Returns `{ current, total, filename } | null`
  </behavior>
  <action>
    1. Create `src/components/import/FindPreviewCard.tsx`:
       - Props: `{ payload: ImportPayload; sourcePath: string; onChange: (p: ImportPayload) => void; onRemove: () => void; }`
       - Thumbnail: if `isHeic(payload.original_filename)` → render a muted `<div>` with a lucide `Image` icon and the text "HEIC preview not supported" (per Research Pitfall 2). Else `<img src={convertFileSrc(sourcePath)} className="h-24 w-24 object-cover rounded" />`.
       - Layout: shadcn `<Card>` with `<CardContent>`. Grid 2 columns: thumbnail left, fields right.
       - Fields: species_name (Input, placeholder "Species name"), date_found (Input type="date"), country (Input), region (Input), lat (Input type="number" step="any", placeholder "Latitude"), lng (Input type="number" step="any"), notes (Textarea, 2 rows).
       - Each input's onChange calls `onChange({ ...payload, <field>: newValue })`. For lat/lng, parse via `parseFloat`, allow empty → null.
       - Top-right 'X' button (lucide X icon) calls `onRemove`.
       - If `payload.date_found === ''`, show a red muted text under the date field: "Date required before import".
       - Use `import { convertFileSrc } from '@tauri-apps/api/core';`.
    2. Create `src/components/import/useImportProgress.ts`:
       - `export function useImportProgress(enabled: boolean): ImportProgress | null`
       - Inside, `useEffect` that runs only when `enabled` is true:
         - `const unlistenPromise = listen<ImportProgress>('import-progress', (e) => setProgress(e.payload));`
         - cleanup: `return () => { unlistenPromise.then(fn => fn()); };`
       - Uses `import { listen } from '@tauri-apps/api/event';`
       - Reset `progress` to null when `enabled` flips false.
    3. Write tests per <behavior> using `@testing-library/react` (already in Phase 1's stack). For useImportProgress, mock `@tauri-apps/api/event` listen in the test file — add a new mock in tauri-mocks.ts or inline `vi.mock`.
    4. Run `npm test -- --run` — new tests green, prior tests still green.
  </action>
  <verify>
    <automated>npm test -- --run</automated>
  </verify>
  <done>
    FindPreviewCard renders editable fields with HEIC placeholder branch, useImportProgress listens + cleans up, all tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ImportDialog wiring (picker → EXIF pre-fill → preview list → Import All → progress) with tests</name>
  <files>
    src/components/import/ImportDialog.tsx,
    src/components/import/ImportDialog.test.tsx
  </files>
  <behavior>
    ImportDialog.test.tsx (all with mocked invoke + mocked dialog.open):
    - Rendering with `open={true}` shows two buttons: "Pick Photos" and "Pick Folder"
    - Clicking "Pick Photos" calls `dialog.open({ multiple: true, filters: [{ name: 'Images', extensions: ['jpg','jpeg','png','webp','heic','heif'] }] })` and for each selected path invokes `parse_exif`, then renders one FindPreviewCard per selection
    - Clicking "Pick Folder" calls `dialog.open({ directory: true })` and the dialog enumerates image files from the folder (via a `read_dir` Rust call — SEE action step for how this is handled in Phase 2)
    - EXIF-populated fields pre-fill date/lat/lng; country/region/species stay empty
    - Editing a card's species propagates (state lives in the dialog, not the card)
    - "Import All" button is disabled while any card has empty `date_found`
    - Clicking "Import All" invokes `import_find` with the current payloads + storagePath from appStore
    - While importing, a Progress bar renders showing `current/total` and filename from the `import-progress` event
    - On completion, the dialog shows a toast "Imported N · Skipped M" and closes (tests assert the close callback was invoked)
    - Error from import_find shows an Alert and does NOT close the dialog

    Folder enumeration decision: Phase 2 does NOT add a Rust `list_images` command (kept for scope). Instead, the folder picker uses `@tauri-apps/plugin-fs` `readDir` (already unlocked via Plan 01 Task 3 adding fs:default to capabilities). Filter results by SUPPORTED_EXTENSIONS in JS.
  </behavior>
  <action>
    1. Create `src/components/import/ImportDialog.tsx`:
       - Props: `{ open: boolean; onOpenChange: (open: boolean) => void; }`
       - Uses shadcn `<Dialog>`, `<DialogContent>`, `<DialogHeader>`, `<DialogTitle>`, `<DialogFooter>`.
       - Local state: `pending: Array<{ sourcePath: string; payload: ImportPayload }>`, `importing: boolean`, `error: string | null`.
       - Import `import { open as openDialog } from '@tauri-apps/plugin-dialog';`
       - Import `import { readDir } from '@tauri-apps/plugin-fs';`
       - `handlePickFiles`: `const selected = await openDialog({ multiple: true, filters: [{ name: 'Images', extensions: [...SUPPORTED_EXTENSIONS].map(e => e.replace('.','')) }] });` — if array, for each path call `parseExif(path)` and build an initial payload with original_filename = path.split('/').pop()!, date_found = exif.date ?? '', lat = exif.lat, lng = exif.lng, species_name = '', country = '', region = '', notes = '', source_path = path. Append to `pending`.
       - `handlePickFolder`: `const dir = await openDialog({ directory: true });` → if string, `const entries = await readDir(dir);` → filter by extension → same per-file pre-fill loop as above.
       - Preview list: `{pending.map((item, i) => <FindPreviewCard key={i} payload={item.payload} sourcePath={item.sourcePath} onChange={(p) => updateAt(i, p)} onRemove={() => removeAt(i)} />)}`
       - `const progress = useImportProgress(importing);`
       - Import button:
         - Disabled if `pending.length === 0` OR any `p.payload.date_found === ''` OR `importing === true`.
         - `onClick`:
           ```
           setImporting(true);
           try {
             const summary = await importFind(storagePath!, pending.map(p => p.payload));
             toast.success(`Imported ${summary.imported.length} · Skipped ${summary.skipped.length}`);
             setPending([]);
             onOpenChange(false);
           } catch (e) {
             setError(String(e));
           } finally {
             setImporting(false);
           }
           ```
       - Progress bar: when `importing && progress`, render shadcn `<Progress value={(progress.current/progress.total)*100} />` with muted text `{progress.current}/{progress.total} · {progress.filename}`.
       - Error Alert above the footer when `error` is set.
       - `useAppStore(s => s.storagePath)` for the storagePath.
       - Toast: use the existing `sonner` instance if present in Phase 1, else `import { toast } from 'sonner';` (add `sonner` to package.json if missing — VERIFY before action).
    2. Add a temporary "Import Photos" button to `src/tabs/CollectionTab.tsx` (MINIMAL change — a single shadcn Button that toggles `ImportDialog` open state via local React state, rendered above the EmptyState). Plan 03 will replace CollectionTab entirely. This is intentional scaffolding so end-to-end manual testing is possible after Plan 02.
    3. Write `ImportDialog.test.tsx` covering all behaviors in <behavior>. Mock `@tauri-apps/plugin-dialog`, `@tauri-apps/plugin-fs`, and the invoke handlers. Progress event: call `mockIPC`'s emitter or directly invoke the listen callback via a mocked listen.
    4. Run `npm test -- --run` — all Plan 02 tests green, Phase 1 baseline green.
    5. Checkpoint: this task leaves CollectionTab as a temporary scaffold. Plan 03 replaces it.
  </action>
  <verify>
    <automated>npm test -- --run</automated>
  </verify>
  <done>
    ImportDialog opens file + folder pickers, pre-fills EXIF, renders editable preview cards, "Import All" invokes Rust command with progress events, completion toast shows, error Alert branch works, tests green. CollectionTab has temporary Import Photos button for manual smoke.
  </done>
</task>

</tasks>

<verification>
After all three tasks:
- `npm test -- --run` → Phase 1 baseline (24) + all new Plan 02 tests green
- `grep -n "parse_exif\|import_find\|get_finds" src/lib/finds.ts` → all three wrapped
- `grep -n "convertFileSrc" src/components/import/FindPreviewCard.tsx` → thumbnail pattern in place
- `grep -n "import-progress" src/components/import/useImportProgress.ts` → event name match
- Manual smoke (human verify, documented in 02-VALIDATION.md): launch app, click Import Photos, pick a real JPEG with GPS, confirm preview fields pre-fill, import, confirm file appears in the correct StorageRoot/Country/Region/date folder
</verification>

<success_criteria>
- Dialog opens both file and folder pickers.
- EXIF date/GPS pre-fills the preview card.
- All fields editable; empty date blocks the Import button.
- Rust `import_find` is invoked with the exact payload shape.
- Progress bar updates from `import-progress` events via `useImportProgress`.
- HEIC thumbnails show a placeholder, not a broken image.
- Completion toast reports imported + skipped counts.
- Vitest suite fully green.
</success_criteria>

<output>
After completion, create `.planning/phases/02-import-organization/02-02-SUMMARY.md` with:
- Components added and their test count
- How the dialog wires picker → preview → import → progress
- Confirmation that CollectionTab has a temporary import trigger (to be replaced in Plan 03)
- Any deviations (especially if folder enumeration approach had to change)
</output>
