# Lightbox header overlap summary

## Changed

- Converted the lightbox metadata header to a two-column grid so the species name and action area cannot overlap.
- Allowed long species names to wrap inside the left column.
- Made the inline delete confirmation stack into a compact two-line control.

## Verified

- `npm.cmd run build`
