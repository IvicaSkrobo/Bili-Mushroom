---
phase: quick-260706-k2m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/popover.tsx
  - src/tabs/CollectionTab.tsx
  - src/tabs/CollectionTab.test.tsx
  - src/i18n/index.ts
autonomous: true
requirements: [Q-01, Q-02]

must_haves:
  truths:
    - "The primary 'Uvezi fotografije' (Import photos) button is never clipped/pushed off-screen at normal desktop window widths"
    - "Location search and date filter controls remain fully functional, now collapsed behind a Filters popover trigger with an active-state indicator dot"
    - "New find and Import photos buttons are pinned together at the right edge of the toolbar"
  artifacts:
    - path: "src/components/ui/popover.tsx"
      provides: "Popover, PopoverTrigger, PopoverContent, PopoverAnchor wrapping radix-ui's Popover primitive, styled consistent with dialog.tsx conventions"
      contains: "PopoverPrimitive"
    - path: "src/tabs/CollectionTab.tsx"
      provides: "Toolbar restructured: flex-wrap safety net, location+date filters collapsed into a Filters popover, New find/Import pinned via ml-auto trailing group"
      contains: "SlidersHorizontal"
---

<objective>
Fix the Collection tab (Zbirka) toolbar overflow reported by the user via screenshot — the primary "Uvezi fotografije" (Import photos) CTA was being clipped off-screen because every toolbar control (search, location search, date filter group, sort toggle, select, favorites, new find, import) sat in one non-wrapping flex row that regularly exceeded window width.

Output: new `Popover` UI primitive; location search + date filter group collapsed into a "Filters" popover with an active-state indicator; New find + Import pinned as a trailing `ml-auto` group; `flex-wrap` safety net on the toolbar row; i18n keys for the new Filters button; updated tests to open the popover before interacting with date filter controls.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@.agents/skills/frontend-design/SKILL.md
</context>
