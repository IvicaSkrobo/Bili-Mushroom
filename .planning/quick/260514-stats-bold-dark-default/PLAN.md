# Plan

## Objective
Fix literal markdown asterisks in the Statistics tab and confirm dark mode is the default while preserving the user's saved theme setting.

## Steps
1. Locate Statistics text/rendering that uses markdown-style bold markers.
2. Replace literal `**...**` rendering with proper structured bold UI or plain emphasized spans.
3. Verify theme persistence/default behavior and adjust only if needed.
4. Run a focused build/check.
