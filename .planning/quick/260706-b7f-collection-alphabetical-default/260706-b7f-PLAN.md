---
phase: quick-260706-b7f
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tabs/CollectionTab.tsx
  - src/tabs/CollectionTab.test.tsx
autonomous: true
requirements: [Q-01]

must_haves:
  truths:
    - "Opening the Collection tab (Zbirka) shows species folders sorted alphabetically by default, without the user needing to toggle the sort control"
    - "The Recent/Alphabetical toggle still fully works — switching to Recent restores backend (most-recent-find-date) order"
  artifacts:
    - path: "src/tabs/CollectionTab.tsx"
      provides: "speciesSortMode initial state defaults to 'alpha' instead of 'recent'"
      contains: "useState<'recent' | 'alpha'>('alpha')"
---

<objective>
Change the default `speciesSortMode` in the Collection tab (Zbirka) from `'recent'` to `'alpha'`, per user request after reviewing the newly added Recent/Alphabetical toggle (added in `collection-search-sort-bugs` debug session) — the user wants alphabetical order as the tab's default view rather than most-recent-find-date order, while keeping both modes fully switchable.

Output: one-line default change in `CollectionTab.tsx`, plus updated test expectations in `CollectionTab.test.tsx` for the new default.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@.planning/debug/collection-search-sort-bugs.md
</context>
