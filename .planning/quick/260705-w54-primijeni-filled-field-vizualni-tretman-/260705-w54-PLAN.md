---
phase: 260705-w54-quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/filledFieldStyle.ts
  - src/components/finds/CreateFindDialog.tsx
  - src/components/import/ImportDialog.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Typing into any shared-header field in the Import photos dialog visually marks it as filled (amber border/background, bold font), matching the New Find dialog"
    - "The New Find dialog's filled-field visual behavior is unchanged after extracting the helper"
  artifacts:
    - path: "src/lib/filledFieldStyle.ts"
      provides: "Shared filledClass helper + style constants, exported for reuse"
      exports: ["filledClass"]
    - path: "src/components/finds/CreateFindDialog.tsx"
      provides: "Imports filledClass from src/lib/filledFieldStyle instead of local definition"
    - path: "src/components/import/ImportDialog.tsx"
      provides: "filledClass applied to all shared-header form fields"
  key_links:
    - from: "src/components/finds/CreateFindDialog.tsx"
      to: "src/lib/filledFieldStyle.ts"
      via: "import { filledClass } from '@/lib/filledFieldStyle'"
      pattern: "import.*filledClass.*filledFieldStyle"
    - from: "src/components/import/ImportDialog.tsx"
      to: "src/lib/filledFieldStyle.ts"
      via: "import { filledClass } from '@/lib/filledFieldStyle'"
      pattern: "import.*filledClass.*filledFieldStyle"
---

<objective>
Extract the existing "filled field" visual-treatment helper (`filledClass`) out of `CreateFindDialog.tsx` into a shared `src/lib/` module, then apply it to the "Import photos" dialog's shared-header form fields, which currently have no such treatment.

Purpose: The user reported that filled-in fields are visually indistinguishable from empty placeholders in the Import dialog, even though this was already fixed for the New Find dialog. This closes that gap by reusing the exact same styling logic, per CLAUDE.md's convention of moving shared logic into `src/lib/`.

Output: `src/lib/filledFieldStyle.ts` (new shared module), updated `CreateFindDialog.tsx` (uses shared import, no visual change), updated `ImportDialog.tsx` (filled-field styling applied to 9 fields).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md

<interfaces>
<!-- Current implementation in CreateFindDialog.tsx (lines ~80-92) to be extracted verbatim -->

```tsx
const filledTextClass =
  'border-primary/65 bg-primary/10 font-serif text-[15px] font-semibold shadow-[inset_3px_0_0_var(--primary)] placeholder:font-sans placeholder:text-muted-foreground/45 dark:bg-primary/12';
const filledUiTextClass =
  'border-primary/65 bg-primary/10 font-semibold shadow-[inset_3px_0_0_var(--primary)] placeholder:font-normal placeholder:text-muted-foreground/45 dark:bg-primary/12';
const filledNumericClass =
  'border-primary/65 bg-primary/10 font-mono text-[13px] font-semibold shadow-[inset_3px_0_0_var(--primary)] placeholder:font-sans placeholder:text-muted-foreground/45 dark:bg-primary/12';

function filledClass(value: string, variant: 'text' | 'ui' | 'numeric' = 'text') {
  if (!value.trim()) return undefined;
  if (variant === 'numeric') return filledNumericClass;
  if (variant === 'ui') return filledUiTextClass;
  return filledTextClass;
}
```

Existing call sites in `CreateFindDialog.tsx` (for reference — task 1 only relocates the definition, these call sites keep working via the new import and are NOT otherwise touched):
- Line 852: `className={filledClass(form.species_name)}`
- Line 861: `className={filledClass(form.common_name)}`
- Line 936: `className={cn('ml-2 align-middle', filledClass(form.date_found, 'ui'))}`
- Line 949: `className={filledClass(form.country, 'ui')}`
- Line 958: `className={filledClass(form.region, 'ui')}`
- Line 968: `inputClassName={filledClass(form.location_note, 'ui')}`
- Line 983: `className={filledClass(form.observed_count_range, 'numeric')}`
- Line 998: `className={filledClass(form.lat, 'numeric')}`
- Line 1008: `className={filledClass(form.lng, 'numeric')}`
- Line 1019: `className={filledClass(form.notes)}`
- Line 1029: `className={filledClass(form.species_description)}`

`ImportDialog.tsx` already imports `cn` from `@/lib/utils` (line 30) — reuse it for combining classes where a field already has a `className`.

`LocationNoteInput` (src/components/finds/LocationNoteInput.tsx) accepts a separate `inputClassName?: string` prop (line 58) that lands on its inner `<input>`; `className` goes on the outer wrapper. Use `inputClassName` for the filled treatment, matching `CreateFindDialog.tsx`'s pattern.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract filledClass into shared src/lib/filledFieldStyle.ts</name>
  <files>src/lib/filledFieldStyle.ts, src/components/finds/CreateFindDialog.tsx</files>
  <action>
Create `src/lib/filledFieldStyle.ts` containing the three style constants (`filledTextClass`, `filledUiTextClass`, `filledNumericClass`) and the `filledClass(value, variant)` function, copied verbatim from `CreateFindDialog.tsx` lines 80-92 (see `<interfaces>` above for exact source). Export `filledClass` (named export). The three constants do not need to be exported unless already referenced elsewhere — keep them module-private in the new file.

Then edit `src/components/finds/CreateFindDialog.tsx`:
1. Remove the local definitions of `filledTextClass`, `filledUiTextClass`, `filledNumericClass`, and `filledClass` (lines ~80-92).
2. Add `import { filledClass } from '@/lib/filledFieldStyle';` near the other `@/lib/*` imports (alongside the existing `import { cn } from '@/lib/utils';` and `import { editSourcePhotoImage, ... } from '@/lib/finds';` block, ~line 30-31).
3. Do NOT change any of the 11 call sites — they already reference `filledClass(...)` by name and will resolve via the new import.

This step must produce zero visual or behavioral change to the New Find dialog.
  </action>
  <verify>
    <automated>cd D:\ClaudeProjects\Bili-Mushroom && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "CreateFindDialog\|filledFieldStyle" || echo "no errors in target files"</automated>
  </verify>
  <done>src/lib/filledFieldStyle.ts exists exporting filledClass; CreateFindDialog.tsx imports it instead of defining locally; tsc reports no new errors in either file.</done>
</task>

<task type="auto">
  <name>Task 2: Apply filledClass to ImportDialog.tsx shared-header fields</name>
  <files>src/components/import/ImportDialog.tsx</files>
  <action>
Add `import { filledClass } from '@/lib/filledFieldStyle';` to the import block in `src/components/import/ImportDialog.tsx` (near the other `@/lib/*` imports, e.g. next to `import { reverseGeocode } from '@/lib/geocoding';`). `cn` is already imported from `@/lib/utils` (line 30) — reuse it, do not re-import.

Apply `filledClass` to the following 9 fields inside the "Find metadata form" block (~lines 663-815), matching the exact variant used in `CreateFindDialog.tsx` for the equivalent field:

1. `SpeciesNameEditor` (sharedName, ~line 667-674): add `className={filledClass(sharedName)}`.
2. `Input` for common name (sharedCommonName, ~line 677-690): add `className={filledClass(sharedCommonName)}`.
3. `DateInput` (sharedDate, ~line 713-717): add `className={filledClass(sharedDate, 'ui')}` — keep the existing `aria-label` prop untouched.
4. `Input` for country (sharedCountry, ~line 727-731): add `className={filledClass(sharedCountry, 'ui')}`.
5. `Input` for region (sharedRegion, ~line 735-739): add `className={filledClass(sharedRegion, 'ui')}`.
6. `LocationNoteInput` (sharedLocationNote, ~line 743-748): add `inputClassName={filledClass(sharedLocationNote, 'ui')}` (use `inputClassName`, not `className` — this component routes `inputClassName` to its inner `<input>`).
7. `Input` for observed range (sharedObservedRange, ~line 757-763): this already has `className="max-w-[220px]"` — change to `className={cn('max-w-[220px]', filledClass(sharedObservedRange, 'numeric'))}`.
8. `Textarea` for find notes (sharedFindNotes, ~line 774-781): already has `className="mt-1"` — change to `className={cn('mt-1', filledClass(sharedFindNotes))}`.
9. `Textarea` for species/folder notes (sharedFolderNotes, ~line 786-796): already has `className="mt-1"` — change to `className={cn('mt-1', filledClass(sharedFolderNotes))}`.
10. `Textarea` for species description (sharedSpeciesDescription, ~line 807-812): add `className={filledClass(sharedSpeciesDescription)}`.

(Note: 10 fields listed above — sharedFindNotes and sharedFolderNotes are both Textareas requiring the `cn()` merge; all others as specified.)

Do not modify any other props, layout, i18n keys, or logic in this block. Do not touch `EditFindDialog.tsx` or any other file.
  </action>
  <verify>
    <automated>cd D:\ClaudeProjects\Bili-Mushroom && npm run build 2>&1 | tail -40</automated>
  </verify>
  <done>npm run build (tsc + vite build) completes successfully with no type errors; all 9 fields in ImportDialog.tsx's shared-header form pass filledClass into their className/inputClassName prop, combined via cn() where a className already existed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None | Pure client-side presentational styling change; no new trust boundary crossed, no new user input parsing, no IPC changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-260705w54-01 | Information Disclosure | N/A | accept | No new data handling introduced; change is CSS class selection based on existing already-rendered form values. |
</threat_model>

<verification>
1. `npx tsc --noEmit` (or `npm run build`) passes with no errors introduced in `src/lib/filledFieldStyle.ts`, `src/components/finds/CreateFindDialog.tsx`, or `src/components/import/ImportDialog.tsx`.
2. Grep confirms `CreateFindDialog.tsx` no longer defines `filledTextClass`/`filledClass` locally and instead imports from `@/lib/filledFieldStyle`.
3. Grep confirms `ImportDialog.tsx` imports `filledClass` and applies it to all 9 listed fields.
4. Deferred (out of scope for this plan, note for user): manually open Import dialog, type into each field (Latin name, common name, date, country, region, location note, observed count, find notes, species notes, species description), and visually confirm the amber border/background + bold font treatment appears once a value is present, matching New Find dialog behavior.
</verification>

<success_criteria>
- `src/lib/filledFieldStyle.ts` exists and is the single source of truth for `filledClass`.
- `CreateFindDialog.tsx` behavior/appearance for New Find is unchanged (verified by no logic/JSX changes beyond the import swap).
- `ImportDialog.tsx`'s shared-header form fields (species name, common name, date, country, region, location note, observed count, find notes, species/folder notes, species description) all apply the filled-field treatment when populated.
- `npm run build` passes.
- No other files modified (confirm via `git diff --stat` shows only the 3 files in `files_modified`).
</success_criteria>

<output>
After completion, create `.planning/quick/260705-w54-primijeni-filled-field-vizualni-tretman-/260705-w54-SUMMARY.md`
</output>
