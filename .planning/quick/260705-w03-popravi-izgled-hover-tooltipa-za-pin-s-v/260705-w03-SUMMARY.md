---
phase: quick-260705-w03
plan: 01
subsystem: ui
tags: [react-leaflet, css, tooltip, map]

# Dependency graph
requires: []
provides:
  - Multi-species map pin hover tooltip that stacks species names vertically (one per line) instead of collapsing into a single row
affects: [map, collection-pins]

# Tech tracking
tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified:
    - src/components/map/CollectionPins.tsx
    - src/index.css

key-decisions:
  - "Used explicit inline flex-column layout on the tooltip's inner wrapper div rather than relying on implicit block-level div stacking, since Leaflet's base .leaflet-tooltip CSS forces white-space: nowrap on the tooltip container"
  - "Replaced white-space: nowrap with white-space: normal on .bili-species-tooltip (explicit, not unset) so the rule does not inherit nowrap behavior from Leaflet's base tooltip class"

patterns-established: []

requirements-completed: [QUICK-260705-W03]

# Metrics
duration: 8min
completed: 2026-07-05
---

# Quick Task 260705-w03: Fix Multi-Species Hover Tooltip Layout Summary

**Multi-species map pin hover tooltip now stacks species names vertically via explicit flexbox column layout instead of collapsing into one row under Leaflet's default `white-space: nowrap`.**

## Performance

- **Duration:** 8 min
- **Started:** 2026-07-05T20:58:00Z
- **Completed:** 2026-07-05T21:05:49Z
- **Tasks:** 1 completed
- **Files modified:** 2

## Accomplishments
- Hovering a map pin representing multiple species at the same coordinate now shows each species name on its own line
- Fix is purely presentational — species grouping logic (`locationGroupsFromFinds`) and pin label generation were untouched
- Existing dark Forest Codex tooltip styling (background, border, radius, shadow, text color, arrow) preserved exactly

## Task Commits

1. **Task 1: Stack tooltip species list vertically with explicit flex layout** - `d195f04` (fix)

**Plan metadata:** (to be committed separately by orchestrator)

## Files Created/Modified
- `src/components/map/CollectionPins.tsx` - Tooltip wrapper `<div>` inline style now includes `display: 'flex', flexDirection: 'column', gap: '2px'` alongside existing font styling
- `src/index.css` - `.bili-species-tooltip` rule: `white-space: nowrap` replaced with `white-space: normal`; all other properties (background, border, border-radius, color, box-shadow, padding, `::before` arrow color) unchanged

## Decisions Made
- Kept `white-space: normal` explicit (rather than removing the declaration entirely) so the rule does not silently inherit `nowrap` from Leaflet's base `.leaflet-tooltip` class, per the plan's root-cause analysis.

## Deviations from Plan

None - plan executed exactly as written.

## Self-Check: PASSED

- FOUND: src/components/map/CollectionPins.tsx (flex column style present, verified via diff)
- FOUND: src/index.css (`white-space: normal` present, verified via diff)
- FOUND: commit d195f04 in git log
- `npm run build` (tsc + vite build) completed successfully with no new errors

## Verification

`npm run build` ran successfully (tsc type-check + vite build), confirming no TypeScript or build errors were introduced. Manual visual verification (hovering a multi-species pin in the running app) is deferred to the user, as specified in the plan.
