# Summary

## Completed
- Widened import-photo dialog to `max-w-5xl` with viewport-safe width.
- Widened edit-find/photo dialog to `max-w-5xl` with viewport-safe width.
- Widened manual new-find dialog to `max-w-4xl` with viewport-safe width.
- Let photo thumbnail grids use more columns on large screens.

## Verification
- `npm.cmd run build`
