# Species suggestions prefix-only matching

## Goal

When typing in species autocomplete, show suggestions that start with the typed letters instead of entries whose common name merely contains the letters somewhere in the middle.

## Scope

- Update shared species query matching.
- Preserve autocomplete sorting and metadata search.
- Add focused tests for prefix-only common-name matching.

## Verification

- Run species name tests.
- Run frontend build.
