---
phase: quick
plan: 260705-w7r
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tabs/StatsTab.tsx
  - src/lib/insights.ts
  - src/lib/insights.test.ts
  - src/i18n/index.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Stats tab no longer renders a 'Seasonal Insights' / 'Sezonski uvidi' card with generic strong-month text"
    - "'This Time in Past Years' (U ovo doba prošlih godina) section still renders unchanged"
    - "The Compass hint (spotHint) below still renders unchanged"
    - "npm run build succeeds with no TypeScript errors"
    - "npx vitest run src/lib/insights.test.ts passes with no failures"
  artifacts:
    - path: "src/lib/insights.ts"
      provides: "buildSpeciesSpotHint only (buildSeasonalityInsights and SeasonalityInsight removed)"
    - path: "src/tabs/StatsTab.tsx"
      provides: "Stats tab without Seasonal Insights block or seasonalityInsights useMemo"
    - path: "src/lib/insights.test.ts"
      provides: "Tests for buildSpeciesSpotHint only"
  key_links:
    - from: "src/tabs/StatsTab.tsx"
      to: "src/lib/insights.ts"
      via: "import { buildSpeciesSpotHint }"
      pattern: "buildSpeciesSpotHint"
</must_haves>
---

<objective>
Remove the redundant "Seasonal Insights" (Sezonski uvidi) section from the Stats tab. This section shows generic, low-value text (e.g. "June is historically strong") that fully overlaps with the existing "This Time in Past Years" section, which already surfaces real historical activity from actual data.

Purpose: Eliminate redundant, low-value UI and dead code without touching the surviving "This Time in Past Years" section or the separate Compass spot-hint feature.

Output: `StatsTab.tsx`, `insights.ts`, `insights.test.ts`, and `i18n/index.ts` updated to fully remove the Seasonal Insights feature while leaving all other Stats tab features intact and working.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/tabs/StatsTab.tsx
@src/lib/insights.ts
@src/lib/insights.test.ts

Investigation already completed (do not re-explore the codebase for this — the scope below is exact and verified):

- `buildSeasonalityInsights` and `SeasonalityInsight` (src/lib/insights.ts) are used ONLY by the Seasonal Insights block in StatsTab.tsx and by the `describe('buildSeasonalityInsights', ...)` test block in insights.test.ts. No other usages exist anywhere in `src/`.
- `buildSpeciesSpotHint` and its helpers (`markedSpeciesName`, `localMonthName`, `extractMonthNum`) are used by the surviving Compass hint feature and MUST NOT be removed.
- i18n keys `stats.insightStrong`, `stats.insightMostActive`, `stats.insightLogged`, `stats.seasonalInsights` are used ONLY in `src/i18n/index.ts` (hr + en blocks), `src/lib/insights.ts`, `src/tabs/StatsTab.tsx`, and `src/lib/insights.test.ts` — all files being edited in this plan. No other usages exist.
- `renderMarkedText` helper in StatsTab.tsx MUST remain (still used by the surviving spotHint Alert).
- `bestMonths` / `speciesStats` hooks in StatsTab.tsx MUST remain (used by RankedList "Best Months" and per-species list, independent of the removed useMemo).
- `src/components/stats/SeasonalCalendar.tsx` is unrelated and out of scope — do not touch it.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove buildSeasonalityInsights from insights.ts and its tests</name>
  <files>src/lib/insights.ts, src/lib/insights.test.ts</files>
  <action>
In `src/lib/insights.ts`:
- Remove the `SeasonalityInsight` interface.
- Remove the `buildSeasonalityInsights` function entirely.
- Keep `Translator` type, `markedSpeciesName`, `localMonthName`, `extractMonthNum`, and `buildSpeciesSpotHint` exactly as-is — verify each helper is still referenced by `buildSpeciesSpotHint` before leaving it in place (all four currently are: `extractMonthNum` and `markedSpeciesName` and `localMonthName` are called inside `buildSpeciesSpotHint`).

In `src/lib/insights.test.ts`:
- Remove `buildSeasonalityInsights` from the import statement (line 2), keep `buildSpeciesSpotHint`.
- Remove the entire `describe('buildSeasonalityInsights', ...)` block (all three `it(...)` cases).
- Keep the `describe('buildSpeciesSpotHint', ...)` block untouched.
- In the `t` mock helper's `templates` object, you may optionally remove the now-unused `stats.insightStrong`, `stats.insightMostActive`, `stats.insightLogged` entries — keep `stats.spotHint` (still used by the surviving test).
  </action>
  <verify>
    <automated>cd "D:\ClaudeProjects\Bili-Mushroom" && npx vitest run src/lib/insights.test.ts</automated>
  </verify>
  <done>insights.ts exports only buildSpeciesSpotHint (plus its private helpers); insights.test.ts contains only the buildSpeciesSpotHint describe block; vitest run passes with no failures.</done>
</task>

<task type="auto">
  <name>Task 2: Remove Seasonal Insights block from StatsTab.tsx and unused i18n keys</name>
  <files>src/tabs/StatsTab.tsx, src/i18n/index.ts</files>
  <action>
In `src/tabs/StatsTab.tsx`:
- Change the import on line 19 from `import { buildSeasonalityInsights, buildSpeciesSpotHint } from '@/lib/insights';` to `import { buildSpeciesSpotHint } from '@/lib/insights';`.
- Remove the `seasonalityInsights` useMemo block (currently around lines 177-180):
  ```tsx
  const seasonalityInsights = useMemo(
    () => buildSeasonalityInsights(bestMonths, speciesStats, locale, t),
    [bestMonths, speciesStats, locale],
  );
  ```
- Remove the entire "Seasonal Insights" JSX block (currently around lines 440-455):
  ```tsx
  {/* Seasonal Insights */}
  {seasonalityInsights.length > 0 && (
    <div>
      <h3 className="text-base font-bold uppercase tracking-[0.12em] text-foreground mb-4">
        {t('stats.seasonalInsights')}
      </h3>
      <div className="grid grid-cols-2 gap-4">
        {seasonalityInsights.map((insight) => (
          <div key={insight.title} className="rounded-lg border border-border/70 bg-card/60 p-4">
            <p className="text-sm font-semibold text-primary">{renderMarkedText(insight.title)}</p>
            <p className="mt-1 text-sm text-muted-foreground">{renderMarkedText(insight.body)}</p>
          </div>
        ))}
      </div>
    </div>
  )}
  ```
- Do NOT remove `renderMarkedText`, `bestMonths`, `speciesStats`, `speciesSpotHint`, or the surviving "This Time in Past Years" / Compass hint JSX blocks — they must render exactly as before.

In `src/i18n/index.ts`:
- In the hr (Croatian) translation block, remove the entries for `stats.insightStrong`, `stats.insightMostActive`, `stats.insightLogged`, and `stats.seasonalInsights`.
- In the en (English) translation block, remove the same four keys.
- Do not touch `stats.spotHint` or any other `stats.*` key.
  </action>
  <verify>
    <automated>cd "D:\ClaudeProjects\Bili-Mushroom" && npm run build</automated>
  </verify>
  <done>StatsTab.tsx no longer imports or references buildSeasonalityInsights/seasonalityInsights/stats.seasonalInsights; i18n/index.ts no longer defines the four removed keys in either locale block; npm run build (tsc + vite) completes with no errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None — this is a pure UI/dead-code removal with no new trust boundaries, no user input handling changes, and no data flow changes.

## STRIDE Threat Register

No new threats introduced. This plan only deletes existing, already-reviewed local-only display logic (no network calls, no persisted data changes, no external input parsing).
</threat_model>

<verification>
1. `npm run build` succeeds (tsc + vite build) with zero errors — confirms no dangling imports/references to removed symbols anywhere in the codebase.
2. `npx vitest run src/lib/insights.test.ts` passes — confirms buildSpeciesSpotHint behavior is unaffected and no stale test references the removed function.
3. Grep confirms zero remaining references to `buildSeasonalityInsights`, `SeasonalityInsight`, `stats.seasonalInsights`, `stats.insightStrong`, `stats.insightMostActive`, `stats.insightLogged` anywhere in `src/`.

Deferred (out of scope for this plan, left for user): manual visual verification — opening the Stats tab in the running app to confirm the Seasonal Insights card is visually gone and the "This Time in Past Years" section and Compass hint still render/behave correctly.
</verification>

<success_criteria>
- Seasonal Insights feature (component code, derivation function, tests, i18n strings) is fully removed with no dead code or dangling references left behind.
- "This Time in Past Years" section and Compass spot-hint feature are untouched and functionally identical to before this change.
- `npm run build` and `npx vitest run src/lib/insights.test.ts` both pass.
- No files outside the four listed in `files_modified` are touched.
</success_criteria>

<output>
After completion, create `.planning/quick/260705-w7r-ukloni-sezonski-uvidi-sekciju-iz-stats-t/260705-w7r-SUMMARY.md`
</output>
