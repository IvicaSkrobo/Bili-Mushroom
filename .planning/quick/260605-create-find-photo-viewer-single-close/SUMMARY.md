# Create find photo viewer single close summary

## Changed

- Removed the custom close button from the selected-photo viewer toolbar.
- Kept the standard dialog close button, so the viewer now shows one close control instead of two.

## Verified

- `npm.cmd run build`
