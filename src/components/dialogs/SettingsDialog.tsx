// Stub — replaced by Task 2 with real implementation
export interface SettingsDialogProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}

export function SettingsDialog(_props: SettingsDialogProps) {
  return null;
}
